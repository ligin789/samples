export type TenantConfig = "airCraftAi" | "default";
const ASSET_FOLDER = "airCraftAi";

// JSON Files Mapping
const jsonFiles: any = {
  airCraftAi: {
    // mapData: require("../assets/jsons/mapData.json"),
    //import json here
  },
};

// Images Mapping //svg add .default for geting actual path


// Function to Get JSON Data
export const getJsonData = (key: any) => jsonFiles[ASSET_FOLDER]?.[key] || null;
