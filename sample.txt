import React, { useState } from "react";
import "./login.scss";
import { AUTH_URL } from "../../services/http/service";

function LoginScreen({ history, noAuthorization }) {
  const [loading, setLoading] = useState(false);
  console.log("Inside login page:::")
  const login = (e) => {
    console.log("Inside login page e 456::",e)
    e.preventDefault();
    setLoading(true);
    sessionStorage.clear();
    window.location.href = AUTH_URL;
  };

  return (
    <div className="login">
      {/* No authorozation */}
      {/* {noAuthorization ? (
        <div className="login-container">
          <h1>Unable to Login</h1>
          <div className="login-sub-container">
            <p>You've been unable to login.</p>
            <p>
              You are unable to log in to smartmx because you are not an admin, however you may still be logged
              in to your single sign-on identity provider using your admin credentials.
            </p>
            <p>
              Trying to use admin credentials ? 
              <a onClick={(e) => login(e)}>
                {loading ? "Loading ... " : "Log in again."}
              </a>
            </p>
          </div>
        </div>
      ) : ( */}
        <div className="login-container">
          <h1>Logged out</h1>
          <div className="login-sub-container">
            <p>You've been logged out.</p>
            <p>
              You've been logged out of smartmx, however you may still be logged
              in to your single sign-on identity provider.
            </p>
            <p>
              Didn't mean to log out?
              <a onClick={(e) => login(e)}>
                {loading ? "Loading ... " : "Log in again."}
              </a>
            </p>
          </div>
        </div>
      {/* )} */}
    </div>
  );
}

export default LoginScreen;
