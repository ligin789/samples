 renders the application without crashing (17 ms)

  ● renders the application without crashing

    Cannot find module './index' from 'test/indexMain.test.tsx'

      17 |   document.body.appendChild(root);
      18 |  
    > 19 |   require('./index'); // Import the file to trigger execution
         |   ^
      20 |  
      21 |   expect(ReactDOM.createRoot).toHaveBeenCalledWith(root);
      22 | });
