export type TenantConfig = "airCraftAi" | "default";
const ASSET_FOLDER = "airCraftAi";


// Images Mapping //svg add .default for geting actual path
const imageFiles: any = {
  airCraftAi: {
    closeButton: require("../assets/images/closeButton.png"),
    //import img like this use svg
  },
};



// Function to Get Image Path
export const getImage = (key: any) => imageFiles[ASSET_FOLDER]?.[key] || null;
