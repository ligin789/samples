import React from 'react'
 
const AvianaLabel = (props:any) => {
    const {onClick=null,className="",style=null,text=""} = props;
    return (
        <label onClick={onClick} className={className} style={style}>
            {text}
        </label>
    )
}
 
export default AvianaLabel
