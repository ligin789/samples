encrypt › should generate a base64 encoded SHA-256 hash

    ReferenceError: crypto is not defined

      16 | export async function encrypt(str:any) {
      17 |     const bytes = new TextEncoder().encode(str);
    > 18 |     const hash = await crypto.subtle.digest('SHA-256', bytes);
         |                        ^
      19 |     return toBase64(hash);
      20 | }
