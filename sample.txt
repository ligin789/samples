import { ColorVarible } from "./tenantConfig";
let primaryColor = "",
  SecondaryColor = "",
  whiteColor = "#fff",
  blackColor = "";

switch (ColorVarible) {
  case "airCraftAi":
    (primaryColor = ""),
      (SecondaryColor = ""),
      (whiteColor = "#fff"),
      (blackColor = "#000");

    break;
    
  default:
    break;
}

const GlobalColor = {
  primaryColor,
  SecondaryColor,
  whiteColor,
  blackColor,
};

export default GlobalColor;
