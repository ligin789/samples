 //set station data for header
    let stationDataLocal:any=[];
    data.map((option:any)=>
    {
      option.flights.map((subOption:any)=>
      {
        stationDataLocal.push(subOption.carrierCode+subOption.flightNumber)

      })
    })
    setStationData(stationDataLocal)
