  .dropdown-headerview::-webkit-scrollbar {
    width: 2px;
    height: 8px;
  }
    
  /* Handle */
 .dropdown-headerview::-webkit-scrollbar-thumb {
    border-radius: 10px;
  }

<div
                className="dropdown-headerview"
                style={{}}
                id="scrollableDiv"
                ref={scrollableDiv}
                onClick={() =>
                  !selectedOptions.length &&
                  dropDownIndex !== -1 &&
                  handleDropDownIndex(-1)
                }
              >
                {data.length > 0 &&
                  selectedOptions.map(
                    (option: any, index: number) =>
                      option.name !== "ALL" && (
                        <div
                          style={{
                            backgroundColor: option?.isMandatory
                              ? Colors.columnsSelectedNotClickable
                              : Colors.columnsSelected,
                            color: Colors.common,
                          }}
                          key={option.name || option.key}
                          className={
                            option?.isMandatory
                              ? "header-item-disable"
                              : "header-item"
                          }
                        >
                          <span>{t(option.tKey) || option.key}</span>
                          <span
                            className="crossIcon"
                            onClick={() =>
                              !option.isMandatory &&
                              removeSelectedColumn(
                                index,
                                option.name || option.key
                              )
                            }
                          >
                            ✕
                          </span>
                        </div>
                      )
                  )}
              </div>
