import React from "react";
import DynamicRender from "../dynamicrenderer";
import { getJsonData } from "../../global/TurnEndConfig";

export const WeatherHoverPopupNotam = (props:any) => {
    return (
        <div style={{ zIndex: 1000, zoom: "var(--zoomPercentage)", marginTop: -14 }}>
            <DynamicRender
                data={getJsonData("WeatherPopUpNotamJson")}
            />
        </div>
    )
}
export default WeatherHoverPopupNotam
