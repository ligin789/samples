const express = require("express");
const app = express();
const port = 3000;
const appRouter = app.router();

appRouter.get("/item", async (req, res) => {
 
});

app.listen(port, () => {
  console.log(`Server running at http://localhost:${port}`);
});
