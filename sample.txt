import React from 'react';
import './index.css'; // External CSS file for styles (optional)
import Colors from '../../global/Color';
import AvianaDiv from '../microComponents/avianaDiv';
import AvianaLabel from '../microComponents/avianaLabel';
 
const LangDropdown = (props:any) => {
  return (
    <AvianaDiv className='lang-drodown-container' style={{backgroundColor:Colors.white,boxShadow:`0px 0px 60px ${Colors.langDropDownContainerShadow}`}}>
      {props?.langArray.map((option, index) => (
        <AvianaDiv key={index} className='lang-label-container' style={{borderBottomColor:Colors.langDropDownBorder}} onClick={()=>props.changeLanguage(option)}>
          <AvianaLabel className='lang-name' style={{color:Colors.themeText}} text={option?.name}/>
        </AvianaDiv>
      ))}
  </AvianaDiv>
  );
};
 
export default LangDropdown;
