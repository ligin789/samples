'GetMicroComponent' cannot be used as a JSX component.
      Its return type 'Element | undefined' is not a valid JSX element.
        Type 'undefined' is not assignable to type 'Element | null'.

    36     const { getByText } = render(<GetMicroComponent {...props} />);
                                         ~~~~~~~~~~~~~~~~~



// import {View} from 'react-native';
import GetMicroComponent from '../../utils/GetMicroComponent';
const ViewComponent = (props: any) => {

  if(props.styles.hasOwnProperty("border-bottom-width")){
    // console.log("TextComponent", props);
  }
  const {
    styles,
    childComponent,
    data,
    mappedFunctions,
    visible,
    mainListIndex,
  } = props;
  return (
    visible && (
      <div style={styles}>
        {childComponent?.children?.map((nextChildComponent: any) => {
          return GetMicroComponent({
            childComponent: nextChildComponent,
            data: data,
            mappedFunctions,
            mainListIndex,
          });
        })}
      </div>
    )
  );
};

export default ViewComponent;
