import React from 'react'
import PropTypes from 'prop-types'
import './AvianaButton.css'
 
const AvianaButton = (props:any) => {
    const {onClick=null,className="",style=null,disabled=false} = props
    return (
        <button style={style} disabled={disabled} onClick={onClick} className={`${className} btn`}>
            {props?.children}
        </button>
    )
}
 
export default AvianaButton
